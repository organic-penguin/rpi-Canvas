install apache2
install php
install fbi
fbi -noverbose -m 640x480 -a -u -t 6 -T 2 /home/pi/Pictures/*
npm install --unsafe-perm sleep

<Directory /home/pi/Pictures/>
        Options Indexes FollowSymLinks MultiViews
        AllowOverride All
        Require all granted
</Directory>

Alias "/home/pi/Pictures/" "/home/pi/Pictures/"

sudo a2enmod userdir

https://unix.stackexchange.com/questions/115054/php-shell-exec-permission-on-linux-ubuntu


create a file that has the number of seconds to wait before switching
have the fbi script reference that for the time
and then have a settings section that overwrites all text in it